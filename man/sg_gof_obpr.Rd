% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sg-gof-obpr.R
\name{sg_gof_obpr}
\alias{sg_gof_obpr}
\title{Create observed vs predicted plot}
\usage{
sg_gof_obpr(
  fpath_i,
  cov_cols = NULL,
  indiv = T,
  addline = T,
  alpha_i = 0.5,
  smooth = T,
  log_axes = F,
  sc_factor = 1,
  abreaks = scales::pretty_breaks(7),
  lab_x = "Model-predicted values",
  lab_y = "Observed values",
  col_i = NULL,
  col_lab = NULL,
  facet_i = NULL,
  f_scales = "fixed",
  no_leg = F,
  n_quantiles = 3,
  levels_discrete = 10
)
}
\arguments{
\item{fpath_i}{string or sg-fit object. If the string is given, the path to \code{.Rdata} file with sg-fit object is expected}

\item{cov_cols}{vector of characters. Name of the columns with covariates}

\item{indiv}{logical. If \code{TRUE} uses individual predictions (\code{"IPRED"}); otherwise uses population predictions (\code{"PRED"}). Default is \code{TRUE}}

\item{addline}{logical. If \code{TRUE}, lines connecting observations of individual subjects will be added. Default is \code{TRUE}}

\item{alpha_i}{numeric. Transparency level (from 0 to 1) for points/lines. Default is 0.5}

\item{smooth}{logical. Add LOESS smooth line. Default is \code{TRUE}}

\item{log_axes}{logical. If \code{TRUE}, a logarithmic scale is applied to all axes. Default is \code{FALSE}}

\item{sc_factor}{numeric. Scaling factor for DV/PRED/IPRED values. Default is 1 (no scaling)}

\item{abreaks}{Axis breaks function. Default is \code{scales::pretty_breaks(7)}}

\item{lab_x}{X-axis label. Default "Model-predicted values"}

\item{lab_y}{Y-axis label. Default "Observed values"}

\item{col_i}{string. Column name for color}

\item{col_lab}{string. Label for color legend}

\item{facet_i}{string. Column name for facet}

\item{f_scales}{one of \code{"fixed"}, \code{"free"}, \code{"free_x"}, \code{"free_y"}. User can specify whether the scales (x and y axes) should be fixed across all panels (\code{"fixed"}), free for each panel (\code{"free"}), or free only in one dimension (\code{"free_x"} or \code{"free_y"}). Default is \code{"fixed"}}

\item{no_leg}{logical. If \code{TRUE}, no legend will be displayed. Default is \code{FALSE}}

\item{n_quantiles}{integer. Number of quantile groups for continuous variables in \code{col_i}. Default is 3}

\item{levels_discrete}{integer. Maximum unique values to consider a variable discrete. Default is 10}
}
\value{
A ggplot2 object
}
\description{
Creates a scatter plot of observed (DV) vs predicted (PRED/IPRED) values
with options for faceting, coloring by covariates, and trend lines.
}
\examples{
\donttest{
# Basic plot
p <- sg_gof_obpr("PK.RData")
# With covariates
p <- sg_gof_obpr(
  "PK.RData",
  cov_cols = "SEX",
  col_i = "SEX",
  facet_i = "SEX"
)
}

}
